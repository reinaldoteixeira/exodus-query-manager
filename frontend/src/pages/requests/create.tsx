import Default from '../../components/layouts/Default/Default';
import RequestCreatePage from '../../components/templates/RequestCreate';

const RequestCreate: React.FC = () => (
  <Default>
    <RequestCreatePage />
  </Default>
);

export default RequestCreate;
