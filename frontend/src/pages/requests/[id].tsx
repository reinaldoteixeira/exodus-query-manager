import Default from '../../components/layouts/Default/Default';
import RequestDetailsPage from '../../components/templates/RequestDetails';

const RequestDetails: React.FC = () => (
  <Default>
    <RequestDetailsPage />
  </Default>
);

export default RequestDetails;
